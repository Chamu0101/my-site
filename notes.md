# 2026-09-09

## やったこと

- GitHubアカウントを作り直した（Chamu0101）
- ブラウザだけでリポジトリを作り、index.html を書いて GitHub Pages で公開した
- git config で名前とメール（noreplyアドレス）を設定
- Homebrew をインストール
- GitHub CLI（gh）をインストールして認証
- リポジトリを Mac にクローン（~/dev/my-site）
- VS Code をインストール、フォルダを開いて編集
- add → commit → push で公開URLに反映されるのを確認した
- 誤字（「を」抜け）を直して2回目のコミット
- JavaScript でボタンを押すと文字が変わるようにした
- クリック回数を数えるように改造した

## 詰まったこと・どう抜けたか

**古いGitHubアカウントのメールが使えなかった**
メールアドレスの追加にも旧アドレスへの認証が必要で完全に詰んだ。
→ まだ中身がHTML1枚だけだったので、アカウントごと作り直した。
「作り直すコストが一番安い日」が今日だった。半年後だったら諦めていたと思う。

**nano から抜けられなくなった**
Control + O を押すと ø が出る。option ではなく control を押しているはずなのに。
→ 原因は未解決。Command + Q でターミナルごと終了して、VS Code に切り替えて回避した。
後で調べる。

**Open Folder で index.html が薄くて選べないと思った**
→ フォルダを選ぶ画面なので、ファイルが薄いのは正常だった。
何も選ばずに「開く」を押せばよかった。

**count = count + 1 の意味がわからなかった**
算数だとありえない式に見えた。
→ プログラミングの `=` は「等しい」ではなく「右側を計算して左の箱に入れろ」という命令。
count が 0 のとき、右側の 0 + 1 が計算されて 1 になり、それが count に入る。

**console.log = count と書いてしまった**
→ 正しくは `console.log(count)`。console.log は箱ではなく、呼び出して使う道具。
変数の中身を確認するデバッグの必需品らしい。

## わからなかった言葉

PATH / シェル / .zprofile / リポジトリ / クローン / コミット / プッシュ /
ブランチ / main / Homebrew / CLI / noreplyアドレス / DOM /
addEventListener / イベント / スコープ / 代入 / const と let の違い / コンソール

## 明日やること

- 2ページ目（works.html）を作って、index.html とリンクで行き来できるようにする
- Progate の JavaScript の続き。習ったことは自分のリポジトリで白紙から書き直す
- Control キーで ø が出る件を調べる
- インボイスの判断と開業届（今月中。期限が近い）

## 2026-09-10

### やったこと

- 昨日のJavaScriptのカウンターを、白紙から書き直そうとした
- works.html（実績ページ）を新規作成。index.html を見ながら自分で全部書いた
- 両方のページに `<nav>` を入れて、ホーム↔実績を行き来できるようにした
- HTMLのコメント `<!-- -->` を覚えた
- push して公開

### 詰まったこと

- 白紙から書けたのは `let count = 0;` と `count = count + 1;` だけ。
  `document.getElementById` `addEventListener` `textContent` は1文字も出てこなかった
- `<nav>` を `</body>` の外に書いていた。表示されるものは body の中に入れる
- `<a href="">建築会社HP</a>` でリンクがクリックできなかった。
  原因はタグの中身が空だったこと。href は行き先、中身が見た目。別物
- `href` を `file:///Users/...` にしていた。これは自分のMacの中の住所で、他人は開けない
- `lang="en"` のままだった

### 気づいたこと・決めたこと

- **理解したものは残って、コピペしたものは消えた。** 昨日20分悩んだ部分だけが今日書けた
- nav を index.html と works.html の両方に同じ内容で書いた。
  ページが増えたら全部直すことになる。WordPress や Next.js が存在する理由はこれだと思う
- **実績1件目は自分の屋号サイトではなく、妻のネイルサロンのサイトにする。**

### 次回

- パソコンを開く前に、妻へのヒアリング（誰に来てほしいか／何をしてほしいか／載せるもの／写真／期日）

## 2026-09-12

### やったこと

- プロゲート：配列 / for / length / オブジェクト を進めた
- practice.js で白紙から課題を書いた
  ネイルサロンのメニュー表（オブジェクトを要素に持つ配列 → for で整形して出力 → 件数と合計金額）

### 詰まったこと

- for の `( )` の後に `;` を書いてしまい、ループの中身が空になった。中身は `{ }` で囲む
- `console.log` を for の外に置いて `index is not defined`。
  `let` で作った変数は `{ }` の中でしか生きていない（9/9のカウンターと同じスコープの話）
- price を `"6000円"` と文字列で持ってしまった。
  データは素の値で持ち、単位は表示時に付ける。文字列だと足し算が連結になる
- `+` での文字列の組み立て方が分からなかった。1段ずつ足して毎回実行すれば分かる

### 気づいたこと

- 「for は分岐」だと勘違いしていた。**for は繰り返し、if は分岐**
- 配列の「要素」と `getElementById` の「Element」は別物。訳語が同じだけ
- 直書き（ハードコード）しない。件数は `menu.length`、合計は `total` に計算させる
- `menu.length` はどこにでも置けるが `total` は for の後でないと `0` になる。
  **コードは上から下に流れる時間**で、その行が動く時点で値が確定しているかで置ける場所が決まる

  ### 所感

今日が一番充実した。分からないことがはっきり見えて、自分で質問できたから理解が進んだ。
白紙から書くのは面倒だが、いま一番大事なのはこれだと思う。

### 次回

- 妻へのヒアリング（誰に来てほしいか／何をしてほしいか／載せるもの／写真／期日）
- 平日に税務署（開業届・インボイスの判断／9月末が期限）

## 2026-09-13

### やったこと

- プロゲート：undefined / if / === / テンプレートリテラル
- practice.js でメニュー表に「価格未設定＝要相談」の分岐を実装（最後は答えを見た）

### 詰まったこと

- `const item = menu[i]` を for の外に置いた。`index` はその時点でまだ存在しない
- `for` の閉じ括弧 `}` を忘れた → SyntaxError: Unexpected end of input
- `${}` をシングルクォートで囲んだ。テンプレートリテラルはバッククォート必須
- price に `"要相談"` を入れてしまった。**これは表示の文言であってデータではない**
  （9/12の `"6000円"` と同じ間違い。データと表示は分ける）
- **console.log を書き換えず、書き足してしまった。** 古い行が残って原因が見えなくなった

### 気づいたこと

- 表示が2通りに分かれるときは、**先に「表示用の文字」を変数に決めてから、出力は1回だけ**
- 作り直すときは古い行を先に消す。怖ければコメントアウト
- インデントを揃えていないと、括弧の過不足が目で見つけられない

## 2026-09-14

### やったこと

- practice.js を消して、昨日の課題を白紙から再現（完走。答えは見ていない）
- 書いたコードを1行ずつ日本語で説明した

### 詰まったこと

- 配列に `item` と名付けて、for の中で `const item = menu[i]` と衝突した。
  **複数のものが入る箱は複数形（menu）、取り出した1つは単数形（item）**
- `let total = 0;` を for の中に置いた
- `${}` の中に日本語を入れてしまった。`${}` に入るのは変数、外はそのまま出る文字

### 説明できた / できなかった

- 説明できた：配列の役割 / `const item = menu[i]` / `total` の加算が else にある理由
- あやふや：`let total = 0` が for の外にある理由（言葉がズレた）
- あやふや：`let priceText;` を空で宣言する理由

→ どちらも「その行がいつ実行されるか」の話。次回の的

## for の整理

### forとは何か

配列（集まり）の全員に、同じ処理をする道具。
繰り返す回数は人間が決めない。データが決める。だから `menu.length` を使う。

### 使うタイミング

自分の言葉に複数形が出たら for。
「全部の〜」「〜一覧」「それぞれの〜」「〜たちを調べて」

判断の決め手は **その件数は将来変わりうるか**。
- 変わる（メニュー・実績・顧客・商品）→ for
- 変わらない（サイト名・電話番号）→ そのまま書く

1個しかないものには使わない。ボタンのカウンターに for が無かったのはそのため。

### 3つの部分

```javascript
for (let i = 0; i < menu.length; i++) {
     ①初期化   　 ②条件式        ③更新
```

- ① 最初に1回だけ
- ② 毎周、中身を実行する前にチェック。false になったら終了
- ③ 毎周、**中身を実行したあと**に i を1増やす

### 実行の順番（menu.length が 4 のとき）
中身が動くのは4回。i は 0,1,2,3。`menu[0]`〜`menu[3]` でちょうど全件。

ここから3つ説明できる。

- `let total = 0;` を中に置くとダメ → 毎周リセットされる
- `const item = menu[i];` は中でいい → 毎周入れ替わってほしい
- `i <= menu.length` はダメ → `menu[4]` を取りに行って undefined

### 一番大事なこと

**for は「繰り返すための道具」ではなく、
「件数が変わっても壊れないようにするための道具」。**

配列に1件足しても、for は書き換えない。
コードは1行のまま、出力だけが5行に増える。
→ コードの量とデータの量が切り離される。

コピペで4行書いても動くが、5件目になった瞬間に黙って消える。エラーも出ない。

### 記号の復習
## 2026-09-15

### やったこと

- forのドリル（i が 0→1→2→3 と動くのを目で確認）
- 変数の置き場所と初期値で、壊れ方がどう変わるかを3パターン実験した

### 実験の結果

同じバグでも、書き方によって壊れ方が変わる。

| 書き方 | 壊れたときに出るもの | 気づけるか |
|---|---|---|
| `let priceText;` を for の中 | undefined | 一発で気づく |
| 初期値 `= "要相談"` を入れる | 全部「要相談」 | 気づきにくい |
| `let priceText;` を for の外 | **前の行の価格** | ほぼ気づけない |

3つとも**エラーは出ない**。全部「動いて」しまう。

### 分かったこと

- **宣言と代入は別の行為。** `let` が宣言（生きる範囲を決める）、`=` が代入（値を入れる）。
  `let total = 0;` は2つを同時にやっている。`let priceText;` は宣言だけ
- 変数の置き場所の判断：**前の周の値を次の周に持ち越したいか**
  - 持ち越したい → for の外（total）
  - 持ち越したくない → for の中（item, priceText）
- 外に出すと、代入が漏れた周に**前の行の値が残って表示される**
- 初期値を入れると、壊れたときに**それっぽい値**が出て気づけない
  → **壊れたときに壊れたと分かる書き方をする**
- `let` を書かずに代入すると、Nodeでは動くがグローバル変数になる。
  実務の厳格モードではエラー。「今動く」と「本番で動く」は別

### まだ混ざるところ

- `priceText` と `total` を混同しがち（2回やった）
  - `item.price` = データ（計算に使う）
  - `priceText` = 表示（画面に出すだけ）
  - `total` = 計算結果
  - **priceText は計算に一切関わっていない**

### 気づいたこと

汚いコードとは、動かないコードではなく**動くけれど後で困るコード**のこと。
今日見た3パターンは全部動いていた。

綺麗なコードと呼ばれるものの中身は4つ。
①読んで意味が分かる ②増えても壊れない ③同じことを2箇所に書かない ④壊れたとき壊れたと分かる

判断基準は「3ヶ月後の自分が、これを直せるか」

### 配列とオブジェクトの使い分け（リファレンス）

判断は3問。

1. **複数あるか** → `[ ]`
2. **1つのものの、いろいろな情報か** → `{ }`
3. **迷ったら：番号で呼んで意味が通じるか**
   - `closedDays[0]` → 通じる（どれも同じ曜日）→ 配列
   - `businessHours[0]` → 通じない（開店か閉店か分からない）→ オブジェクト

補助の見分け方

- **同じ種類**が並ぶ → `[ ]` ／ **違う役割**が集まる → `{ }`
- **件数が増減する** → `[ ]` ／ **キーが固定** → `{ }`

掘り方：`{ }` を持っていたら `.名前`、`[ ]` を持っていたら `[番号]`。

## 2026-09-16

### やったこと

- 復習：`let total = 0;` が for の外にある理由 / `let priceText;` が for の中にある理由
- プロゲート「関数を学ぼう」全9セクション完走
  （関数とは / アロー関数 / 引数 / 複数の引数 / 戻り値 / 戻り値の活用 / スコープ / 総合演習）

### 詰まったこと

- **`priceText` と `total` をまた混同した（3回目）**
  - `total = total + item.price;` に priceText は登場しない
  - priceText を外に出しても合計は正しいまま。崩れるのは**表示**だけ
  - **覚え方：名前に `Text` が付いているものは画面用。計算には使わない**
- `console.log("130の半分は65です")` と直書きした。作った `result` を使っていない
  → 9/13に priceText を作って使わなかったのと同じ。**新しい変数を作ったら、使ったか確認する**
- `return result hour*60 + minute;` と書いた。
  `return` の後ろには**返したい値だけ**。呼び出し側がどんな名前で受け取るかを、関数は知らない
- 関数の中に「呼び出し」と「代入」を書いてしまった。
  **関数の中は「計算して返す」だけ。呼び出し・代入・出力は全部外**

### 分かったこと

**戻り値**

- **関数を呼んだ場所が、返ってきた値に置き換わる（化ける）**
  `const result = half(130);` → `const result = 65;` と同じ
- `menu.length` も `item.name` も同じ動き。値を返すものは全部その場が値に化ける

| | やること | その後 |
|---|---|---|
| `console.log(x)` | 画面に出す | 終わり。再利用できない |
| `return x` | 呼び出し元に値を返す | **受け取って使える** |

`console.log` は出しっぱなし、`return` は手渡し。
手渡された値は変数に入れたり、別の関数に渡したりできる。**だから関数は組み合わせられる**

**true/false を返す関数**

- `if (check(123))` のように、**関数呼び出しをそのまま条件式に置ける**
- `=== true` は不要（`true === true` になって無駄）
- 慣習として `is〜` `has〜` という名前を付ける

**スコープの向き**

> **内側から外側は見える。外側から内側は見えない。**

`{ }` の中で作ったものは、その `{ }` が閉じたら消える。
for でも if でも関数でも同じルール。
9/9のカウンター、9/13の `const item`、9/15の `priceText` ——全部これだった

### 気づいたこと

- **データの重複は配列で解決した。処理の重複は関数で解決する**
- 変数だけでは足りない理由：メニューが100件あったら変数を100個作ることになる。
  **たくさんのものを1つの名前で扱うために入れ物がある**
  - **配列 = 同じものの「並び」**（名簿）
  - **オブジェクト = 1つのものの「内訳」**（履歴書1枚）
  - 名簿とは履歴書の束 → だから配列の中にオブジェクト
- JavaScriptは同じ原理が形を変えて出てくる。土台ができると後半が速くなる

### 次回の課題（未着手）

`practice.js` を関数を使う形に書き直す。

- `formatPrice(price)` … undefined なら "要相談"、そうでなければ "◯◯円" を返す
- `getTotal(menu)` … 配列を受け取って合計を返す

書けたら昨日のコードと for の中身を比べる（5行 → 1行になるはず）

### 事務手続き（判明したこと）

- 開業届・青色申告は**「人」に対する手続き**。配達で済んでいるので**新事業でも再申請不要**
- ただし**屋号（Chamu）を登録するには開業届の再提出が必要**
  - 屋号名義の銀行口座が開ける → 配達とHP制作の入金が分かれて記帳が楽になる
  - 請求書の振込先が個人名でなくなる
  - 備考欄に「屋号の追加登録」、職業欄に両方の事業を書く
  - **タイミングは事業開始時（10〜11月）。1件目の請求書を出す前まで**
- インボイスは**取引先が決まってから判断でいい**
  - 取引先が免税事業者（零細店舗）なら不要／課税事業者（中小企業）なら求められる可能性
  - 2026年10月から控除率80%→70%、2割特例→3割特例
  - A1/A4フェーズ（零細店舗）は不要な可能性が高い。A2以降で必要になる

### 学習の見通し

| 範囲 | 難易度(10段階) |
|---|---|
| 配列 / for / length | 3 |
| オブジェクト / 入れ子 | 3.5 |
| スコープ / 変数の置き場所 | 4 |
| **関数** | **5** ← 今日 |
| DOM操作 | 4〜5 |
| コールバック / map・filter | 6 |
| **非同期（Promise / async / fetch）** | **8** ← 最大の壁 |

非同期が必要になるのは**A2フェーズ（12〜24ヶ月）**から。
A1（Web制作）とA4（GAS自動化）は今の範囲でほぼ戦える。

**実務が学習の順番を決めてくれる。** ただし
①最初の1〜2件は学習時間込みで見積もる（制作3日・調べもの3日）
②案件に出てこない範囲が空白になるので、土台の学習を週数時間は残す

## 2026-09-21

### やったこと

- サイト構成案を奥さんに見せる用にまとめた（前日の続き。猫・FAQ・子ども同伴の条件を反映）
- CSSの練習を開始。`salon-test.html` でサロンサイトのファーストビューを試作

### 今日覚えたCSS

**画面いっぱいの高さ**

```css
.hero { height: 100vh; }
```

`vh` = viewport height。`100vh` で画面の高さぴったり。PCでもスマホでもその画面に合う。

**ブラウザの初期CSSを打ち消す**

```css
* { box-sizing: border-box; }
body { margin: 0; }
```

何も書かなくてもブラウザは独自のCSSを当てている。`body` には初期で `margin: 8px` が入っていて、
これが「画面いっぱいにしたのにスキマが出る＋スクロールバーが出る」原因だった。
これを打ち消すのを**リセットCSS**という。

**Flexbox（並べ方の指定）**

```css
.hero {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: 20px;
}
```

- `display: flex` は**親に書く**。並ぶのは子
- 初期値は横並び。縦に並べるには `flex-direction: column`
- `justify-content` = **並んでいる方向**の位置（列全体をどこに置くか）
- `align-items` = **それと直角の方向**の位置（一つ一つをどう揃えるか）
- `flex-direction` を変えると、この2つの役割が入れ替わる
- 「justify は横」と覚えると必ず詰まる

**余白は1つの仕組みで管理する**

```css
.hero h1, .hero p { margin: 0; }   /* 打ち消し */
.hero { gap: 20px; }               /* 余白を作るのはここだけ */
```

`gap` と `margin` の両方で余白を作ると足し算になって、「なぜこの隙間？」が分からなくなる。
`margin: 0` は余白を**作っていない**ので混ぜたことにならない。
`gap` が使えるのは Flexbox と Grid の中だけ。それ以外は `margin` で調整する。

**ボタンの作り方**

```css
.btn {
  background-color: #c08a85;
  color: #fff;
  padding: 14px 32px;      /* 上下 左右 */
  text-decoration: none;   /* リンクの下線を消す */
  border-radius: 999px;
}
```

ボタンの大きさは `width` で決めない。**`padding` で内側から押し広げる。**
そうすれば文字が長くなってもボタンが自動で伸びる。

**レスポンシブの基本**

```css
width: 600px       /* ✗ 絶対に600px。画面が狭いとはみ出す */
max-width: 600px   /* ○ 最大でも600px。狭ければ縮む */
```

「スマホで崩れている」の半分は、どこかに `width` が書いてあるせい。

### 分かったこと

- **すべての要素は「箱」**。CSSは箱に対して指示している（ボックスモデル）
  - 中身 → padding（内側の余白）→ border（枠線）→ margin（外側の余白）
  - `max-width` は箱を**作っている**のではなく、もとからある箱に**上限をつけている**
- **CSSは入れ子にしない。** HTMLは入れ子にするが、CSSのルールは独立して並べる
- **親子＝入れ子の関係。** `.hero h1` の半角スペースは「〜の中にある」
- **Flexbox は一列に並べる。Grid は格子に並べる。** どちらも親に書く
- CSSの書き方は3つ（インライン／`<style>`／外部ファイル）。実務は外部ファイル。
  理由は9/10に自分で気づいた「navを2ファイルに書くと全部直すことになる」と同じ

### 道具まわり

- **Prettier** を入れて、デフォルトフォーマッタに設定した
- **Format On Save / Format On Paste** をON → `Command + S` で保存と整形が同時
- **保存しても整形されない＝構文が壊れているサイン**（括弧の過不足など）
- 開発者ツール：**右クリック → 検証**が一番確実
- スマホ表示：開発者ツール左上の**スマホマーク**（`Command + Shift + M`）
- Elements で要素を選ぶと、右下に**ボックスモデルの図**が出る。
  オレンジ=margin／黄=border／緑=padding／青=中身。隙間の原因を探すときはここを見る
- VS Code と GitHub を連携した。ただし**gitはあと2〜3週間ターミナルで続ける**。
  ボタンが何をしているか分からないまま押すと、詰まったとき直せないため

### 次回

1. `salon-test.html` を白紙から書き直す（復習）
2. hero の下にセクションをもう1つ足す（縦に積む・余白・横幅制限）
3. Grid でギャラリーを3列に